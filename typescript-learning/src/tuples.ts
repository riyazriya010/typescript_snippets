let arr: [string, number, boolean]
arr = ['1', 3, true]

let rgb: [number, number, number] = [234, 125, 984]

type User = [string, number]
const user: User = ['123', 345]

user[0] = 'riyas'
// user.push(true)  this will throw the error

export{}

